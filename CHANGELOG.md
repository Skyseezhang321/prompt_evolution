# Changelog

本文件用于持续记录 `prompt_evolution` 仓库中的重要变更，便于回溯 prompt 优化、自进化实验、分析方法与结论的演进过程。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，但在仓库形成稳定版本节奏前，先按日期维护。

## 记录规则

- 将尚未发布或尚未归档的变更写入 `## Unreleased`。
- 每次阶段性整理、实验完成、论文/笔记重构或版本发布时，将 `Unreleased` 内容归档到新的日期小节。
- 只记录对研究追踪有意义的变更，避免记录纯格式化、拼写修正等噪声。
- 每条记录尽量说明“变更内容”和“影响范围”，必要时补充关联实验、数据集、prompt 策略或评估指标。

## 变更类型

- `Added`: 新增研究材料、实验、脚本、数据、prompt 模板或评估方法。
- `Changed`: 调整研究结构、prompt 优化策略、实验流程、分析口径或文档组织。
- `Fixed`: 修正实验代码、数据处理、结论表述或复现实验中的问题。
- `Deprecated`: 标记后续不再推荐使用的 prompt、方法、实验流程或文档。
- `Removed`: 移除过时材料、无效实验、废弃脚本或不再使用的数据。
- `Security`: 与密钥、隐私、数据脱敏或外部服务访问相关的修复。

## Unreleased

<!-- 将下一批重要变更写在这里。 -->

### Added

- 各报告/文档页头部加**「💬 对话助手主页」导航按钮**：`scripts/build_doc_html.py` 粘顶栏首位新增茶青实底按钮（href 按页面目录深度算相对路径，file:// 与任意静态托管直接可达）；v3 综合报告 `docs/analysis_report_v3_20260610.html` 页面导航 nav 首位加同款按钮（沿用该页自身配色变量）。后端托管形态下 `/docs/*` 页面的相对链接 `../advisor/advisor.html` 不在静态挂载内，`advisor/server.py` 新增 `/advisor/advisor.html` 别名路由兜住，`advisor/test_server.py` +1 条路由契约测试。验证：`python -m pytest advisor/ -q` **27 项全过**；Playwright 实测三个入口（primer 一级目录、知乎洞见卡 source_batches 二级目录、v3 报告）点击按钮均落到对话助手页。影响范围：生成器脚本 + 14 页重建、v3 报告 2 处小改（nav 链接 + 2 行 CSS）、server/test 各一小段；v1/v2 报告为冻结存档未加。
- Prompt 优化建议助手**视觉层重设计**（与文档页同一套「学术期刊 / 研究档案」视觉语言）：`advisor/build_advisor.py` 模板样式块整体替换——暖纸面 + 墨色 + 茶青主色，朱砂印章式助手头像与顶栏（宋体衬线标题 / 黑体正文 / 等宽印章式证据等级标签），内联 SVG 纸面纹理（零外部依赖、file:// 离线可用），消息入场动画、聊天区暖色渐变与细滚动条；LLM 回答里的 `[I0x·等级]` 引用渲染为等宽引用芯片；洞见卡由顶部色条改为左侧色条 + 等宽编号眉头；启用此前未接线的 `__UPDATED__` 占位符、副标题显示知识库更新日期。三栏控制台结构、对话状态机 / SSE 流式 / 检索逻辑与响应式抽屉机制**全部未动**（仅 `.shell` 高度补偿顶栏双线 +2px）。验证：`python -m pytest advisor/ -q` **26 项全过**；Playwright 截图核验首屏 / 建议卡片 / 右栏洞见详情三态渲染。影响范围：`advisor/build_advisor.py` 样式块与一行副标题、`advisor.html` 重新生成；后端、知识库、测试未动。
- 文档 HTML 渲染管线**视觉升级**并把 APO 七法主线详解纳入渲染：`scripts/build_doc_html.py` 主题从基础灰绿配色升级为「学术期刊 / 研究档案」视觉语言——暖纸面 + 墨色 + 茶青主色 + 朱砂点缀，宋体衬线标题 / 黑体正文 / 等宽编号的全系统字体栈（无外部字体/CDN，file:// 离线可用），内联 SVG 纸面颗粒纹理；右侧目录新增 IntersectionObserver 阅读位置高亮（渐进增强、无 JS 时目录仍可用）；粘顶栏加刊头标记与双线分隔。`docs/apo_seven_methods_primer_20260611.md` 加入 PAGES 渲染清单（此前从报告/笔记点过去只能看到裸 markdown）。重跑生成器 14/14 页全部重建（13 既有页换新主题 + 1 新页）。验证：Playwright 对新 primer 页三个滚动位置截图核验（标题/表格/引用块/目录滚动高亮正常）。影响范围：仅 `scripts/build_doc_html.py` 与生成的 `.html`；全部 `.md` 源、证据与结论未动；v3 报告 HTML（手工构建）未动，新主题与其同属 teal 色系、延续而非改换身份。
- 修复建议助手入口页「文档与报告」点开是裸 markdown 的问题：`scripts/build_doc_html.py` 的 PAGES 增加 `popsci_prompt_evolution_story_20260610.md`（中间层标签）并生成 `docs/popsci_prompt_evolution_story_20260610.html`——至此该入口 4 篇 md 全部有主题化 HTML（洞见手册、候选清单、脑图三篇此前已生成）；`advisor/build_advisor.py` 模板 RESOURCES 的 4 条链接由 `.md` 改指 `.html`（脑图条目文案「Mermaid 源」改「渲染版」，因落点已是 mermaid.js 渲染页），重新生成 `advisor.html`。验证：Playwright 实测入口页 5 条 reslink href 全部以 `.html` 结尾、科普页主题渲染正常（粘顶返回栏 + TOC + 标题）。影响范围：1 个生成器脚本加 1 行、1 份新 HTML、advisor 模板 4 行链接 + advisor.html 重新生成；洞见卡片内指向 `docs/paper_notes/*.md` 的证据追溯链接不在本次范围。（`advisor/build_advisor.py` 模板）：原居中单栏聊天 → 顶栏（标题 + LLM 模式 + 重新开始 + 窄屏抽屉开关）+ 三栏 `shell`。**左**＝知识库导览（文档/报告链接 5 条 + 12 洞见按 A–F 分组带证据等级 + 8 反模式 + 6 首批实验，共 26 个可点条目）；**中**＝对话（保留全部既有逻辑：引导问答状态机、流式、向量召回、确定性兜底）；**右**＝详情面板（默认证据等级图例 + 诚实声明；点左侧任意洞见/反模式/实验显示其全文卡；每次 LLM 回答后显示「本轮回答引用的来源」，洞见可点再展开）。响应式：<1180 右栏收为抽屉、<860 左栏也收为抽屉，由顶栏「📚 知识库 / 📄 详情」按钮 + 半透明 scrim 唤出。目标：一个入口看全部内容，不必跳出页面翻文档。验证：`python -m pytest advisor/ -q` **26 项全过**（纯前端模板改动，逻辑测试不受影响）；Playwright 实测桌面三栏渲染（左 5 链接 + 26 条目、点 I06→右详情卡、点反模式→右反模式卡、默认右栏图例）+ 窄屏 toggle 唤出抽屉。沿用 v3 报告配色与卡片样式，视觉一致。影响范围：仅 `advisor/advisor.html` 重新生成（HTML 结构 + CSS + 面板 JS），未改后端、知识库与检索逻辑。
- 为 Prompt 优化建议助手加**向量召回（语义检索）**：`scripts/llm_clients.py` 新增 `embed_openrouter`（OpenRouter `/embeddings`，默认 `baai/bge-m3`，1024 维，复用现有 config/header；实测 OpenRouter 确支持 embeddings 且该 slug 可用）。新增 `advisor/build_vectors.py` 把 12 洞见 + 42 笔记嵌入、L2 归一化生成 `advisor/vector_index.json`（54×1024，565KB）。`advisor/server.py` 新增 `retrieve_vector`（查询嵌入一次 → 与索引余弦排序 → top 洞见 + top 笔记，笔记设相似度下限 `ADVISOR_NOTE_FLOOR` 默认 0.30）与 `retrieve_grounding`（**向量优先、嵌入失败或无索引自动回退 bigram 关键词**），`_prepare` 改用之；`/api/health` 增报 `retrieval`(vector/keyword) 与 `embed_model`。能命中关键词漏掉的语义相关项（实测「死记硬背、泛化变差」→ 洞见06 过拟合）。验证：`python -m pytest advisor/ -q` **26 项全过**（+3 向量测试：索引已载、余弦排序、嵌入失败回退关键词；新增 autouse fixture 默认令嵌入失败以保证测试不触网）；实跑确认 health `retrieval=vector`、语义问题端到端召回相关洞见 + 笔记。影响范围：`scripts/llm_clients.py` 加 1 个嵌入函数与常量（不改既有）、`advisor/` 加向量生成器 + 索引 + 后端检索分发 + 测试；确定性（file://）模式仍用关键词；未改动 docs 与既有 LLM 行为。
- 新增项目内 Claude Code skill `.claude/skills/article-deep-read/SKILL.md`（社交/行业文章深读入库流程编排）：补齐三个阅读 skill 的最后一块——`read-paper`（论文）、`github-repo-audit`（仓库）之外，知乎/公众号/Twitter/X/工程博客等社交行业文章此前无固化流程，advisor 语料集成条目中引用的 `article-deep-read` 名字至此落地。六步编排：`source_inventory.md` 登记检查（复用已有 source_id、duplicate 不建新笔记）→ 三分支获取原文（user_provided / 公开抓取 / 登录墙请用户粘贴，不绕过验证）→ `local_sources/raw/` UTF-8 快照 + SHA256 → 按 `docs/industry_notes/template.md` 写笔记 → inventory/industry_practices/CHANGELOG 三处联动 → 自检清单。与兄弟 skill 同构：只做步骤编排，字段细则指向 template/insight_field_standard/CLAUDE.md 单一来源；唯一新成文内容是来源证据等级 strong/medium/weak 判据表（此前只散落在笔记先例中），并固化「单篇社交文章默认 weak」「单源不出 conclusion」「insight evidence_strength 用 A/B/C/D 且单源默认 D」三条历史上反复走偏的口径。验证：2 用例 × 带/不带 skill 沙盒对照评测（知乎登录墙+用户粘贴仿真文章、公开博客+inventory 已有 candidate 行），程序化检查器（SHA256 实测一致、字段齐全、联动更新）+ 独立 grader 判分：带 skill 18/18 全过，基线 17/18（把匿名数据模糊化文章整体评 medium，违反先例口径）且在未计分维度出现单源自报数字直接写成 conclusion、evidence_strength 单源标 B 两处边界违规；带 skill 平均省 8.8k token、快 41s。评测工件在 gitignore 的 `artifacts/skill_eval/`。影响范围：纯新增 1 个 skill 文件；未改动模板、口径文档与任何既有笔记。
- 为 Prompt 优化建议助手加**流式输出（SSE）**：`scripts/llm_clients.py` 新增 `stream_openrouter_chat` 生成器（复用现有 config/header/url 构造，解析 OpenRouter SSE 的 `data: {...}` 行、yield `delta.content` 文本片段，跳过纯 reasoning 片段，`[DONE]` 收尾）；`advisor/server.py` 新增 `POST /api/chat/stream`，按 `event: meta`(引用清单)→ 多个 `event: delta`(文本片段)→ `event: done`/`error` 推送 SSE，与非流式 `/api/chat` 共用 `_prepare`(检索)/`_resolve_cfg`(含 max_tokens 覆盖)。前端 `advisor.html`（`build_advisor.py` 模板）`llmAsk` 改用 `fetch` 流式读取 `ReadableStream`、手写 SSE 解析，逐字渲染回答 + 闪烁光标（新增 `.cursor` CSS），结束补可追溯引用区；流式失败/不支持自动回退关键词匹配。验证：`python -m pytest advisor/ -q` **23 项全过**（+2 条流式端点测试：meta/delta/done 序列、错误事件）；实跑 `deepseek/deepseek-v4-pro` 确认**逐步到达**（单次回答 795 个 delta 片段、3083 字，首片 ~12s 后陆续到达）、Playwright 实测浏览器端文本从 20→40→98 字渐增 + 末尾引用区渲染。已知：推理模型首片前有 ~10s+ 推理停顿（由打字指示器覆盖）。影响范围：`scripts/llm_clients.py` 加 1 个流式函数（不改既有函数）、`advisor/server.py` 加流式端点与共用 helper、`advisor.html` 重新生成（流式渲染）、`advisor/test_server.py` +2 测试；未改动 docs 与既有 LLM 客户端行为。
- 新增 `advisor/build_corpus.py` + `advisor/corpus_index.json`，并扩展 `advisor/server.py`：把三个阅读 skill（`read-paper`/`github-repo-audit`/`article-deep-read`）的产出**集成进 LLM 问答（方案 A 扩检索语料 + 方案 B 来源指引）**。`build_corpus.py` 扫描 `docs/paper_notes/`（A）、`docs/github_repo_audit_notes/`（B）、`docs/industry_notes/`（取自标证据等级）共 42 篇 vetted 笔记，抽标题/一句话结论/证据等级/出处生成 `corpus_index.json`；后端 LLM 模式在 12 条洞见之外**额外检索这些笔记作补充扎根**（`retrieve_corpus`），系统提示新增「相关一手笔记」块并要求回答用笔记 id + 等级引用（如 `[paper-vista-reflection-dark-2026·A]`），`/api/chat` 的 `cited` 同时返回洞见与笔记引用，`/api/health` 增报 `corpus` 数。方案 B：系统提示与确定性模式无命中提示均在「材料未覆盖某具体论文/仓库/文章」时指引用对应 skill 把它读进库——**问答只检索 skill 产出、不在运行时实时跑 skill**（实时跑会破坏「只用 vetted 材料」的可追溯保证，正是洞见12）。同时把 `ADVISOR_MAX_TOKENS` 默认 1600→**3000**（推理模型 deepseek-v4-pro 会用掉部分 token 推理，避免正文截断），仍可用环境变量覆盖。验证：`python -m pytest advisor/ -q` **21 项全过**（+3 条语料检索/扎根测试）；实跑（问 VISTA 阴暗面）回答**同时引用 `[I04·recent-preprint]` 与 `[paper-vista-reflection-dark-2026·A]`**、用到笔记里 23.81%→13.50% 与解耦细节、守住「数字仅该论文设置下成立、本项目未复现」口径。影响范围：`advisor/` 新增语料生成器 + 索引 + 扩展后端与测试，`advisor.html` 重新生成（无命中提示加 skill 指引）；确定性模式仍只检索 12 条洞见、语料扩展仅作用于 LLM 模式；未改动任何 docs 笔记内容与证据。
- 新增 `docs/paper_notes/paper-prompt-repetition-2025.md` 深读笔记（read-paper skill 首次实战）：闭环 `source_inventory.md` 2026-06-09 登记的用户线索「Google 重复 prompt 提升非思考模型」——定位为 Google Research 论文 *Prompt Repetition Improves Non-Reasoning LLMs*（arXiv 2512.14982，Leviathan/Kalman/Matias，speculative decoding 原班作者）。核心结论：非推理模式下把 `<QUERY>` 原样重复一遍变成 `<QUERY><QUERY>`，7 模型 × 10 benchmark 配置 47/70 显著胜 0 显著负（McNemar p<0.1），且不增加输出长度与时延（重复发生在可并行 prefill 阶段）；NameIndex 上 Gemini 2.0 Flash-Lite 21.33%→97.33%；Padding 对照证明增益来自重复本身而非输入变长；开启 step-by-step 后收益基本消失（5 胜 1 负 22 平）。对本项目定位：纳入「廉价 baseline 变换集」——任何 APO 方法报告增益前应先对比这个零智能变换；笔记附三臂（baseline/重复×2/padding）最小 A/B 计划。原文快照 `local_sources/raw/arxiv_papers/2512.14982/`（PDF 与抽取文本 SHA256 已记录于笔记）。影响范围：`docs/source_inventory.md` 对应行 candidate→noted；`literature_map.md` 按收录口径（APO/进化/自进化主线）暂不增行；证据等级：方法+结果级（Figure 1/4 经渲染核验结构与方向、位图逐格数字未转录），非本项目复现结论。
- 新增项目内 Claude Code skill `.claude/skills/github-repo-audit/SKILL.md`（GitHub 仓库证据级审计流程编排）：把 `docs/github_repo_source_audit_workflow_20260608.md` 已跑通一轮（core4）的审计闭环固化为可触发步骤清单——`source_inventory.md` 登记 source_id → `scripts/github_repo_clone.py` clone 并固定 commit/README/LICENSE SHA256 → `scripts/github_repo_audit.py` 机器扫描出草稿笔记 → 人工审计（源码观察 / 结构迁移推断 / 可采信结论三类表述）→ 带 source_id+commit+文件路径+L0-L4 等级提炼 insight → 回填工作流文档、`source_inventory.md`、CHANGELOG 三处追溯链。skill 只做步骤编排和红线清单（`local_sources/raw/` 不入库、README 主张≠结论、存在 eval 代码≠方法有效、可转实验先进 `experiment_plan.md`），证据等级与洞见提炼规则的权威定义仍留在工作流文档单一来源，避免双份真相漂移；候选仓库发现（`github_repo_discovery.py`）显式不在范围内。动因：第二轮已排队 5 个高优先级待审仓库（gepa-ai/gepa、microsoft/PromptWizard、promptomatix、AutoPrompt、dspyground），该流程将跨 session 重复执行且约束多、易漏。影响范围：纯新增 1 个 skill 文件；未改动脚本、工作流文档与任何既有审计结论。
- 新增 `advisor/server.py` + `advisor/test_server.py` + `advisor/requirements.txt`：Prompt 优化建议助手 **v2 扎根 LLM 问答后端（FastAPI）**。复用 `scripts/llm_clients.py` 调 OpenRouter（`OPENROUTER_MODEL=deepseek/deepseek-v4-pro`，key 读自仓库根 `.env`）；对 `knowledge_base.json` 做**检索增强**（与前端同口径的空格词 + 中文 bigram 打分，命中少时用 spine 通用纪律兜底），构造**受约束系统提示**（只用知识库作答、每条标注 `[I0x·等级]`、知识库未覆盖处如实声明、论文数字=该论文设置下成立、12 洞见尚未在本项目复现），调模型后返回回答 + **可追溯引用条目**。端点：`GET /`（同源托管 `advisor.html`）、`GET /api/health`（探测 LLM 是否就绪）、`POST /api/chat`、`/docs` 挂载仓库 docs 供卡片出处链接可点。前端（`build_advisor.py` 模板）启动探测 `api/health`：后端在且 key 就绪→自动切「LLM 模式」（顶栏标「已接入」、自由提问走后端、渲染回答 + 引用区），否则回退确定性关键词匹配；LLM 调用失败也自动回退。为推理模型单独把输出上限调到 `ADVISOR_MAX_TOKENS`（默认 1600，避免推理吃光 512 预算导致正文截断），不改 `.env` 全局值。验证：`python -m pytest advisor/ -q` **18 项全过**（9 KB + 9 后端，LLM 调用打桩不触网）；并用 `deepseek/deepseek-v4-pro` **实跑**确认端到端可用——回答按场景引用 I01/I03/I04/I06/I07/I08、对"构建评测集"等知识库未覆盖处主动声明、数字（49%、13.50%→87.57%、+12.63）均正确归因；Playwright 实测后端托管页 LLM 模式切换 + 回答渲染 + 引用区。影响范围：`advisor/` 新增后端 + 测试 + 依赖清单，`advisor.html` 重新生成（加 LLM 模式分支）；`.env` 已含 OpenRouter key（gitignored、未入库）；未改动任何 docs 证据与结论。
- 新增 `docs/apo_seven_methods_primer_20260611.md` APO 七法主线详解：仓库各报告与笔记反复引用"APE→ProTeGi→OPRO→DSPy→TextGrad→MIPROv2→GEPA"基线主干，但此前只有逐篇深读笔记、缺一份把七法串成整体叙事的术语介绍文档。本文档按演进顺序逐方法给出定位、机制、代表性结果、留下的洞和深读笔记指针，并收敛两条暗线（反馈信号密度：标量→critique→trace；优化对象升维：单指令→指令+演示→多模块程序）与使用约定（基线锚点谱系而非排行榜、跨论文原始分数不可横比、最小验证计划留在各笔记不重复）。所有数字与七篇 `method-and-results-read` 深读笔记同口径核对（如 APE IQM 0.810 vs 0.749、ProTeGi +15.3%、OPRO GSM8K 80.2 vs 71.8、DSPy GSM8K ~33%→88.3 dev、TextGrad Object Counting 91.9、MIPRO 5/7 任务联合最优、GEPA 45.23→54.85 超 GRPO 48.91 且平均仅 ~3,936 rollouts）。影响范围：`docs/literature_map.md` 快速脉络、`docs/classic_optimizer_methods_comparison_20260610.md` 关联文档、README 当前文档清单各加一处指针；未改动任何深读笔记内容与结论。证据等级：论文级综合（材料全部来自既有深读笔记），非本项目复现结论。
- 新增 `advisor/` 面向用户的 Prompt 优化建议**对话助手**（对话式 UI + 确定性内核 v1）：把 v3 报告的 12 条洞见做成**单一事实来源**知识库 `advisor/knowledge_base.json`（每条含触发规则、证据等级、真实数字与仓库内出处链接、8 个引导问题、反模式表、首批实验 P0–P2），由 `advisor/build_advisor.py` 校验并内联生成自包含 `advisor/advisor.html`——**聊天界面**（助手气泡逐条提问、可点选项可打字、随时「直接给建议」）由确定性状态机驱动：场景答案经触发 DSL 映射到适用洞见 → 以对话气泡输出**分层建议**（快速诊断→可照抄步骤→真实证据带 A/recent-preprint/B 等级与出处→边界）+ 总诊断 + 反模式 + 建议实验 + 三句收尾；之后可自由追问（关键词匹配，中文用 bigram 切词，UI 标注「非 LLM」）。运行时**不调用 LLM**，免费、可静态托管、零幻觉、100% 可追溯；顶栏「LLM 模式：未接入」与气泡内横幅声明「确定性映射非 LLM、论文数字=该论文设置下成立、12 洞见尚未在本项目复现」。`advisor/test_advisor.py` 覆盖 KB 完整性 / 出处文件存在 / 触发 DSL 的 9 条 golden 场景（多 agent→I08·I11、prompt 变长→I06、无评测集→I01 等），`python -m pytest advisor/test_advisor.py -q` 全过；并用 Playwright 实跑验证对话流（选项→下一题→给建议→I08·I09 命中、自由打字「记忆怎么办」→I10、chip「多 agent」→I11）。决策：目标架构=扎根 LLM RAG + 确定性兜底、先引导后自由；本里程碑做对话式外壳 + 确定性内核（最小实验优先），同一份 KB 与聊天前端第二阶段直接复用——v2 走 OpenRouter（`OPENROUTER_MODEL=deepseek/deepseek-v4-pro`，key 已在本地 `.env`、复用 `scripts/llm_clients.py`，需小后端代理避免静态页暴露 key）。影响范围：新增 `advisor/` 目录（首个面向用户的应用代码 + 数据 + 测试），README 看板加「想直接拿优化建议」入口；未改动任何 docs 证据与结论与 `.env.example`（OpenRouter 配置原已就位）。
- 新增项目内 Claude Code skill `.claude/skills/read-paper/SKILL.md`（单篇论文深读流程编排）：把散落在 `docs/arxiv_deep_reading_framework.md`（L0–L3 分层精读与防过度总结规则）、`docs/insight_field_standard.md`（启发字段口径）与 CLAUDE.md（快照 + SHA256 约束）三处的读论文流程固化为一条可触发的步骤清单：查重定位 → `scripts/download_arxiv_papers.py` 取原文快照 → L0–L3 分层精读 → 按模板写笔记 → source_inventory / literature_map / CHANGELOG 登记 → 链接与数字自检；skill 本身只做步骤编排和必填字段清单，细则仍指向上述单一来源文档，避免规则出现两份真身各自漂移。同步把全部 36 篇既有笔记实际都带、但模板缺失的 5 个字段（`local_pdf_path`、`local_pdf_sha256`、`local_text_path`、`local_text_sha256`、`evidence_level`）补进 `docs/paper_notes/template.md`，消除模板与实践的分叉。影响范围：新增 1 个 skill 文件、模板补 5 个字段；未改动任何既有笔记、证据与结论。批量深读 / batch synthesis 显式声明不在该 skill 范围内。
- 新增 `scripts/build_doc_html.py` 文档主题化生成器与 12 篇渠道/中间层「主要内容说明」的 HTML 版本：把综合论述报告 v3 直链的 5 渠道主说明（arXiv 深读 Batch3 综合、arXiv top80 taxonomy、GitHub 渠道综合、其它平台 / Twitter/X / 知乎 insight cards）与核心中间层（读者向手册、Insight/Method 候选清单、字段规范、最终报告结构、实验计划、Mermaid 脑图源）从 markdown 渲染为统一主题 HTML，解决「从报告点进各部分看到的是原始 markdown、视觉与交互极差」的问题。生成页套用 v3 配色：美化表格/代码块、粘顶「← 返回综合报告 v3」栏 + 渠道色标 chip + 右侧自动目录（TOC）；脑图页用 mermaid.js 渲染 mindmap/证据金字塔/工程纪律闭环三图。单一来源仍为 `.md`，改源后重跑脚本即可；转换集内部 `.md` 链接改写为 `.html`，v3 报告对应 31 处链接由 `.md` 改指 `.html`。影响范围：新增 1 个生成器脚本 + 12 份 `.html`，未改动任何 `.md` 源内容、证据等级与结论；论文笔记与分析/证据层文档按本轮约定范围暂仍为 markdown。
- 新增 PROSE 方法笔记 `docs/paper_notes/paper-prose-2026.md` 与六法横向对比 `docs/classic_optimizer_methods_comparison_20260610.md`：用户发现系统报告反复并列 APE/OPRO/EvoPrompt/PromptBreeder/DSPy-style/PROSE，回溯确认前 5 个已有独立深读笔记、**唯独 PROSE 无独立介绍**，且经核对 coin-flip 本地原文（`local_sources/raw/arxiv_papers/2604.14585/paper.txt`）确认 **PROSE 并非外部论文，而是 coin-flip 作者自建的内部基线**（PRompt Optimization via Structured Evolution = 结构化五段分解 role/task/constraints/examples/format + 6 自适应权重算子 + 风险调整 fitness `0.70·均分+0.15·Sharpe+0.15·DRO`），其唯一作用是充当受控反例、证伪"显式 risk-aware 选择有用"（结论：无可测稳健性优势）。PROSE 笔记按 paper_note 模板组织但显式标注来源为 coin-flip Appendix C、不可当独立文献引用；对比文档收敛 5 维（优化对象/反馈信号/搜索机制/是否自指/原论文结果）+ 唯一可公平横比的同框架表（Haiku Table 2、Nova Lite Table 4）+ 6 条可执行结论。影响范围：`docs/paper_notes/paper-coin-flip-2026.md` 第 42 行补一处澄清 PROSE 身份并交叉链接到新两份文档；其余既有笔记与结论未改动。证据等级：方法+结果级（读全 coin-flip Appendix C 与 Table 2/4），PROSE 部分为负结果基线、非本项目复现结论。
- 新增 `docs/popsci_prompt_evolution_story_20260610.md` 小说体科普《别让 AI 自己改作业》：把 v3 报告的 12 条洞见改写成一个创业团队（产品「小蜜」做客服工单 intent/entities/urgency 抽取）踩坑成长的虚构故事，按报告六组主干（值不值得→失败即证据→别被骗→改哪个零件→记忆/多agent→听来的）组织六章 + 楔子 + 尾声三句话。面向非本领域读者降低阅读门槛，同时保留可追溯性：人物/工单/对话明确标注虚构，论文数字（Coin Flip 49%、ProTeGi +15.3%、VISTA 23.81%→13.50%→87.57%、AutoPDL 6.5%→74.0%、PrefPO 14.7x、JTPRO 51.27%→64.46%、MemAPO 70.7% vs 63.6% 等）均与报告同口径，文末附「故事↔真实证据」对照表（带洞见编号、数字、A/recent-preprint/B 等级）与诚实声明（12 洞见尚未在本项目复现、recent-preprint 需独立复现）。影响范围：纯新增一份读者向衍生产出，未改动任何既有证据与结论；作为 v3 报告与读者向洞见手册的通俗演绎入口。
- 新增 `docs/analysis_report_v3_20260610.html` 跨渠道综合论述报告 v3 和 `docs/prompt_evolution_mindmap_20260610.md` 可编辑 Mermaid 脑图源：在 v2 的「读者向 12 洞见主干」之上，按五渠道（arXiv/GitHub/其它平台/Twitter/知乎）最新一轮分渠道整理收敛出三块新内容——①全自包含内嵌 SVG 全景脑图（中心「工程纪律」分出 渠道证据底座 / 12 洞见六组 A–F / 方法·反模式·覆盖缺口·首批验证）；②跨渠道证据金字塔（arXiv·A → GitHub·B → 其它平台·B → Twitter·B* → 知乎·D，含「能证明/不能证明/覆盖缺口/核心文件」矩阵）；③分渠道速览 + 反模式合并 + 覆盖诚实清点（把各渠道刚收敛的结论、方法、反模式、覆盖偏差和追溯链显式呈现，如 GitHub C-06 受限召回 + 正典 optimizer 第二轮 backlog、其它平台真实失败案例覆盖不足、Twitter 120 候选 pending、知乎 ZH-L1→D 线索定位）；每条洞见新增「跨渠道互证」行；Mermaid 源含 mindmap + 证据金字塔 + 工程纪律闭环三图，与报告同口径。脑图 SVG 由一次性布局脚本生成后内联、脚本未入库。影响范围：新增两份产出并把 README 主报告入口前置到 v3，未改动各渠道证据与既有结论；v2/v1 保留作对照与回滚。
- 新增 3 篇综述的 taxonomy 级深读笔记（APO survey `docs/paper_notes/paper-apo-survey-2025.md`、APE survey `paper-ape-survey-2025.md`、Context Engineering survey `paper-context-engineering-2025.md`）和一份**外部完整性校验** `docs/arxiv_taxonomy_completeness_check_20260610.md`：用 3 篇独立综述作参照系核对 `arxiv_top80_taxonomy.md` 的 7 簇，确认在"离散自然语言 prompt 优化"范围内无整块子领域遗漏；查出簇内偏轻子机制（学习型 reward/entropy/NLL 评分、RL/GAN/finetune 候选、token 级编辑、bandit/UCB 与 region-based 筛选、MoE 路由）、需登记的 frontier（task-agnostic/online、constrained optimization、bi-level/thought-driven 推理模型 o1/R1、multi-task/negative-transfer）、以及需显式声明的刻意边界（soft prompt、multimodal、完整 context-engineering 系统）。影响范围：`docs/source_inventory.md` 3 行状态升为 noted；`docs/arxiv_top80_taxonomy.md` 缺口节增校验结论指针；最高优先级单项补读建议为 bi-level/thought-driven 推理模型 prompt 优化（需定向搜索，可能不在 top80 样本内）。综述/taxonomy 级证据，非本项目复现结论。
- 新增 APE、OPRO、DSPy、MIPROv2、TextGrad 五篇经典锚点论文的全文证据级深读笔记（`docs/paper_notes/paper-ape-2022.md`、`paper-opro-2023.md`、`paper-dspy-2023.md`、`paper-miprov2-2024.md`、`paper-textgrad-2024.md`），闭合 `arxiv_top80_taxonomy.md` 自标的"经典锚点未深读"缺口；每篇含本地 PDF/文本 SHA256、方法机制、主结果数字、消融、失败案例、洞见卡片和最小验证计划，并与已有 ProTeGi/EvoPrompt/PromptBreeder/GEPA 等笔记交叉链接，形成 APE→ProTeGi→OPRO→PromptBreeder/EvoPrompt→DSPy→TextGrad→MIPROv2→GEPA 的基线主干。影响范围：`docs/source_inventory.md` 对应 5 行状态由 candidate/skimmed 升为 noted；`docs/arxiv_top80_taxonomy.md` 缺口条目标记闭合；论文级证据，非本项目复现结论。仍待补更早锚点 AutoPrompt/RLPrompt/GrIPS。
- 新增 `docs/insight_field_standard.md` 字段定义规范：把此前散落在 `research_brief.md`、两个深读模板、`insight_method_catalog` 和 `final_report_outline` 里、只有字段名而无统一口径的 insight / conclusion / helpful method / anti-pattern 四类产出，收敛为单一权威——给出区分口径（含 insight↔conclusion↔method 的判别问句）、各类型必填/可选字段、补齐此前缺失的 `conclusion` 独立 schema、统一字段命名映射并标注 catalog 的待收敛点；影响范围：上述四个文档各加一行指向该规范，证据等级仍沿用 `final_report_outline` 的 A/B/C/D，不改动任何已有结论。

- 新增 `docs/insight_handbook_20260609.md` 读者向洞见手册：面向非本领域读者，按学习顺序（要不要优化 → 怎么诊断失败 → 改什么 → 怎么防过拟合和热度误导）把 12 条核心洞见各写成「反直觉点 + 具体示意例子 + 带出处真实数字 + 可照抄步骤 + 边界」，所有 prompt 例子标注示意、数字标注论文出处，并保留与 insight_method_catalog 的对应关系。
- 新增 `docs/analysis_report_v2_20260609.html` 多渠道洞见报告 v2：按洞见而非渠道组织，把 v1 的标签式结论还原为具体 before/after 例子和带出处数字（如 Coin Flip 49% 低于 zero-shot、VISTA 23.81%→13.50%→87.57%、AutoPDL FEVER 6.5%→74.0%、PrefPO 14.7x 长度）；v1 保留作对照与回滚点。
- 新增 MIT 开源协议文件，明确公开仓库的代码和配套文档可复用边界。
- 新增 `CONTRIBUTING.md`、共创工作流文档和 GitHub issue/PR 模板，把外部研究线索、新颖性判断、深读笔记、实验候选和证据沉淀纳入可管理流程。
- 新增项目构建原则文档，并补充 `AGENTS.md` / `CLAUDE.md`，方便 Codex 和 Claude Code 读取仓库工作约束。
- 新增资料搜集计划，明确论文、行业经验、工程框架和失败案例的收集范围、记录字段、覆盖矩阵和 M0 阶段完成标准。
- 新增来源清单，承接资料搜集阶段的候选论文、行业实践、处理状态和待补缺口，并登记第一批联网核验过的种子来源。
- 新增行业经验笔记目录和模板，用于记录知乎、Twitter/X、工程博客、产品文档和事故复盘等重要单一来源。
- 新增本地原文快照留存规范，使用 `local_sources/raw/` 保存不提交的原文，并在笔记中记录路径和 SHA256。
- 新增 Karpathy-inspired Claude Code guidelines 仓库的行业笔记和来源条目，用于后续 coding-agent 行为规则 eval 设计。
- 新增最终报告结构，明确最终交付需要覆盖前沿状态、证据等级、可执行方案、初步实验和风险治理。
- 新增并重构第一版 HTML 多渠道洞见报告页，按 arXiv 论文、GitHub 源码、行业工具、Twitter/X、知乎和其它平台拆解证据贡献，优先呈现有效 insights、可信 conclusions、可复用 helpful methods、反模式和最小验证方式。
- 新增企业微信机器人通知脚本、配置示例、使用文档和单元测试，后续消息通知统一通过该入口发送。
- 新增本地 Git hook 自动通知，commit 后发送提交消息，push 成功后发送远程更新消息。
- 新增 OpenAI / OpenRouter 最小 LLM API 客户端、dry-run/live smoke test、配置示例、使用文档和单元测试，用于后续实验前检查 provider 配置。
- 新增按渠道广搜通用 posts 候选来源的 `scripts/collect_sources.py`、可选环境变量示例和资料搜集计划说明，支持从 Hacker News、DEV、Stack Exchange、RSS、Brave Web Search、X API 和 Reddit OAuth 收集候选元数据，并输出到本地 ignored artifacts 供人工快筛；GitHub 和 arXiv 暂由独立渠道处理。
- 新增 GitHub 仓库发现脚本、使用文档和单元测试，用于通过 GitHub Search API 批量搜集 prompt optimization / prompt self-evolution 相关候选仓库。
- 新增首批 GitHub 仓库候选快筛，解释 raw candidate 噪声来源，并把 85 个候选分为核心深读、周边参考和暂不处理，同时补充创建时间和最近 push 时间分布。
- 新增 GitHub 仓库三层分析文档、结构化 catalog、重点仓库深读和证据矩阵，并将 8 个严格保留仓库登记到来源清单。
- 新增 GitHub 仓库 clone / audit 脚本、单元测试和源码审计流程文档，已对 core4 仓库固定 commit 并生成审计草稿，用于把 GitHub 渠道 insight 提炼从 README 快筛推进到源码证据层。
- 新增 GitHub 仓库候选 insight 证据卡，从 core4 源码审计中提炼 12 条候选方法/经验，并标注证据等级、适用边界和最小实验候选。
- 新增 GitHub 渠道洞见综合文档，按 insight-first 内容整理原则重写 GitHub 渠道 conclusions、helpful methods、反模式、证据边界和最小验证候选。
- 新增 arXiv 候选论文搜索脚本、说明文档和单元测试，用于按渠道广搜 prompt optimization / prompt evolution 相关论文元数据并生成待粗筛清单。
- 新增 arXiv 聚焦筛选脚本和单元测试，用于把广搜候选重排为 100 篇以内的人工快筛优先清单。
- 新增 arXiv top80 论文三层一读分析，包括简要概述、分类 taxonomy / 横向证据矩阵和重点论文详细介绍，用于指导后续深读笔记与最小实验候选选择。
- 新增 arXiv top80 行动手册，把主要论文问题转化为具体工程例子、解决方案、记录字段和 3 个最小实验候选。
- 新增 arXiv top80 洞见与经验总结，把重点论文从方法索引进一步提炼为可复用的现象、机制、经验规则、边界条件和最小验证方式。
- 新增 arXiv 重点论文下载脚本、manifest 和单元测试，把 15 篇优先深读论文的 PDF 与文本抽取结果保存到本地 ignored artifacts，并记录 SHA256、页数和文本长度。
- 新增 arXiv 重点论文全文深读框架，以及 ProTeGi 和 Modular Prompt Optimization 两篇证据化论文笔记，用于把摘要级判断推进到方法、实验、局限和可复现洞见层面。
- 新增 GEPA、SePO 和 SPEAR 三篇重点论文全文深读笔记，并补充首批深读综合判断，把错误信号质量、候选选择、optimizer artifact 管理和最小复现实验变量沉淀为可执行结论。
- 新增 PromptBreeder、EvoPrompt、Scaling Textual Gradients、Textual Gradients are a Flawed Metaphor、PrefPO 和 TextReg 六篇重点论文全文深读笔记，并补充第二批综合判断，用于校正 textual-gradient 机制解释、扩展演化搜索证据、纳入 prompt hygiene / hacking / OOD overfitting 指标。
- 新增 CriSPO、MemAPO、AutoPDL、MASPO、VISTA、Prompt Codebooks、MAPRO、DistillPrompt、JTPRO 等 16 篇 arXiv 重点论文全文深读笔记，并补充第三批综合判断，把 pre-optimization gate、根因假设、exemplar optimization、tool/schema artifact、multi-agent credit assignment、edit-family hygiene 和 memory 过滤沉淀为后续最小实验约束。
- 新增 `insight_method_catalog_20260609.md`，按最新 insight-first 内容整理原则聚合论文、源码和行业材料，形成 12 条核心洞见候选、6 条当前可采信 conclusions、4 个 helpful methods、反模式清单和首批验证优先级，并同步 README 与实验计划入口。
- 新增知乎、Twitter/X 和其它平台候选 posts 的批次快筛与并发分析包，明确 artifact 路径、证据边界、优先候选、追溯字段和后续处理建议，便于多 session 并行分析。
- 新增 Twitter/X 候选 posts 结构化分析，完成 GEPA/DSPy/MIPRO 社媒线索分层、24 条 source card、X 到一手来源追溯表和排除清单，并将 Pydantic GEPA、Promptim、PromptWizard、DSPyground、Promptomatix 等稳定来源回填到来源清单。
- 新增 Twitter/X 社媒线索洞见卡，按 insight-first 原则把 X/Twitter 线索转成 8 条 insight candidates、3 个 helpful method candidates、反模式和最小验证/演示候选。
- 新增其它平台候选来源结构化分析，完成 high/medium 候选证据分层、source cards、工具与工程实践地图，并将 Hugging Face、Arize、LangChain、Langfuse、Humanloop、Weaviate、OPIK 等稳定来源回填到来源清单和行业实践整理。
- 新增其它平台 insight / method cards，把官方文档、cookbook、工具平台和二手博客线索转写为 optimizer intake、prompt version ledger、context-first diagnosis、source triage 等可复用方法、反模式和最小验证候选。
- 新增知乎候选材料三层分析，基于 99 条搜索候选整理快速概述、主题详细分析、重点论文/框架追溯、深读优先级和排除建议。
- 新增知乎候选材料洞见与方法卡片，将知乎批次按最新 insight-first 原则重写为 insight cards、helpful methods、anti-patterns、重点追溯对象和最小验证候选，并明确知乎材料主要作为中文社区理解与误区线索。
- 新增 `pytest.ini`，将项目测试入口限定到 `tests/`，避免本地忽略目录 `local_sources/raw/` 中的第三方源码快照被 pytest 误收集。

### Changed

- 将其它平台（web_search/工具 + 通用社区广搜）渠道对齐到 `docs/insight_field_standard.md` 并补齐覆盖缺口：在 `docs/source_batches/web_search_platform_insight_cards_20260609.md` 把证据等级从自定义 `L1–L4` 收敛到规范 `A/B/C/D`（多源官方工具实践判为 B，仅搜索摘要线索判为 D，并保留 L→A/B/C/D 换算表）；把 WHM-01..04 从散文改写为字段规范的标准 YAML（补齐 `not_recommended_when`、`evaluation_metrics`、`misuse_or_anti_pattern`、`rollback_plan` 等必填具名字段）；新增「结论总览」「反模式」两节做成清晰渠道入口；新增「广搜渠道覆盖与缺口」节，按 artifact 真实清点 web_search(465)/hackernews(~24)/stackexchange(1)/rss(1)/reddit·dev.to(0)/x_api(未跑)，判定通用社区批次以 GEPA 转贴和 Show HN 产品发布为主、净新增有限，并诚实标注真实失败案例/事故复盘覆盖不足（低于覆盖矩阵要求）。同步在 `docs/source_inventory.md` 登记 3 条净新增候选（AWS Bedrock optimizer、Vertex 锁定 prompt 段落 SO 问答、Ask HN「AI evals 半成品」痛点信号），并在 `README.md` 把 insight cards 标为渠道综合入口、analysis 标为证据层。影响范围：仅其它平台渠道相关文档与来源清单，未改动其它渠道证据与结论。
- 将 Twitter/X 渠道三份产出对齐到 `docs/insight_field_standard.md`（该规范晚于这批产出定稿，故此前未对齐）：字段名 `source_trace`→`main_sources`、证据取值 `B-candidate`→规范 `A/B/C/D`（候选状态在证据边界处单独说明，不污染 `evidence_strength`）、反模式表补必填列 `trigger_condition`；新增 5 条 Conclusion Candidates（按 conclusion schema 填 scope/evidence_strength/counterexample，并标注 main_sources 待补与单源不得直接升格）；补齐 `prompt_as_program_spec`、`social_signal_triage`、`vendor_optimizer_evidence_filter` 三张缺失方法卡，将 `tw-insight-02` 的 trace-aware 方法并入 `method-01`、`tw-insight-07` 的 safety 方法标记为 D 级待补；`twitter_web_analysis` 补「候选去向账目」闭合 120 条候选覆盖、补 next_action 核验进度（均 pending）；`twitter_web_parallel_brief` 补 source card `evidence_level`(strong/medium/weak) 与 insight `evidence_strength`(A/B/C/D) 的映射口径。影响范围：仅 Twitter/X 三份文档，未改动其它渠道证据与结论。
- 为 GitHub 渠道补充覆盖边界与跨源互证：在 `docs/github_repo_channel_synthesis_20260609.md` 新增「渠道覆盖与已知偏差」「repo ↔ paper 对照」两节，把"直接 prompt optimizer 效果证据稀缺"结论降级标注为受限召回（无 token、8 查询的冒烟 discovery）+ core4-only 审计造成的选择偏差，并把 3 个 helpful method 归一到统一目录 `insight_method_catalog_20260609.md` 的 HM-* 命名；同步在 `docs/github_repo_insight_cards_20260608.md` 的 GHI-12 补「覆盖边界」。影响范围：仅文档与证据边界标注，不改动既有 insight / conclusion 内容。
- 重排 GitHub 源码审计流程「下一步」：在 `docs/github_repo_source_audit_workflow_20260608.md` 把 `gepa-ai/gepa`、`microsoft/PromptWizard`、`SalesforceAIResearch/promptomatix`、`Eladlev/AutoPrompt` 等正典 optimizer 仓库列为高优先审计对象（高于 strict8 剩余资料型仓库），标注 strict8 剩余项为有意延后，并补充"配 token 重跑完整 discovery"作为校正低召回的步骤。
- 续做 GitHub 渠道文档收敛（跨文档同口径）：在 `docs/insight_method_catalog_20260609.md` 的 C-06 边界补同口径覆盖偏差说明，使统一目录与渠道综合不再各说各话；在 `docs/github_repo_source_audit_workflow_20260608.md` 新增「第二轮待审仓库（正典 prompt optimizer）」表，把 gepa / PromptWizard / promptomatix / AutoPrompt / dspyground 的 source_id、关联论文、优先级和审计重点固化为可执行 backlog；在 `docs/github_repo_analysis_overview_20260608.md` 的初步判断 1 补「"不多"可能是覆盖不足所致」边界。影响范围：仅文档与证据边界标注，不改动既有 insight / conclusion / 方法内容。
- 新增「读者向内容层」并据此更新入口：诊断 v1 报告过度标签化、缺例子、按渠道组织不利于读者理解，改为按读者学习顺序、以具体例子和真实数字为主的洞见手册 + 报告 v2；同步在 README 看板和文档列表前置手册入口，在实验计划中说明手册「首批最小验证」表与 P0–P2 优先级同口径。各渠道文档与论文笔记保持现状作为证据底座，不改动。
- 调整 README 为公开构建首页，将重要分析、分析经过、阶段结果和下一步计划前置展示。
- 明确本轮流程规则变更提交并推送后，后续代码、文档、实验和配置改动必须先在个人/任务分支完成，再通过 PR/合入请求进入 `main`，用于保护 `main` 的可复现基线和 review/回滚边界；并澄清分支粒度为「每个任务/每位贡献者一条」，单条分支可承载多 session、多次批量改动与多次提交，而非每次改动都新建分支。
- 将当前路线从“准备进入最小可复现实验”前移到“M0 资料搜集与综述冻结”，实验计划暂作为后续候选设计。
- 将 M0 资料搜集和实验计划调整为五天交付版，强调前沿状态跟踪、可执行方案产出和 1-2 个关键判断的最小实验验证。
- 将项目主线调整为 insight-first：优先沉淀有效 insights、可信 conclusions、可复用 helpful methods、反模式和风险边界，实验仅作为验证关键洞见、演示方法和校准边界的手段，并同步更新 README、研究框架、资料搜集计划、最终报告结构、实验计划、项目原则、共创工作流和笔记模板。
- 将各渠道总结文档补充为“具体洞见优先”的写作口径，在 arXiv、GitHub、行业实践、Twitter/X、其它平台和知乎分析中新增普通用户可理解的方法卡片、最小验证方式和证据边界，并同步最终报告结构，同时保留原有论文、源码和工具证据层。
- 企业微信命令行和 Git hook 通知默认附带自然语言版 git 主要修改内容，并把涉及文件作为次级明细展示。

## 2026-06-08

### Added

- 新增 changelog 文档，用于跟踪 prompt 优化/自进化研究仓库的重要变化。
- 新增研究总览、文献地图、行业实践、实验计划和论文笔记模板。
